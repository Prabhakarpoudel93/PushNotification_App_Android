<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="androidpractise.com.prabhakar.garbagecollectionsystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View_Notification"></activity>
        <activity android:name=".SelectArea" />
        <activity android:name=".TokenDemo">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".EditCustomer" />
        <activity android:name=".DetailActivity_cust" />
        <activity android:name=".View_Customer" />
        <activity
            android:name=".HomeActivityCust"
            android:theme="@style/AppNewTheme" />
        <activity
            android:name=".AdminDashboard"
            android:theme="@style/AppNewTheme" />
        <activity android:name=".CustomerSignup" />
        <activity android:name=".AdminLogin" />
        <activity
            android:name=".WasteConcern"
            android:label="WasteConcern"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>